import { Container, Typography } from '@mui/material';

export const Home = () => {
	return (
		<Container>
			<Typography variant="h1">Re:Form</Typography>
			<Typography variant="h2">A React Form Management Library</Typography>
		</Container>
	);
};
